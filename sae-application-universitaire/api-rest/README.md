# API REST

API REST PHP servant d'intermédiaire entre les clients (site web, application Java) et la base de données MySQL.

## Mise en place
- Déployer ce dossier sur un serveur PHP.
- Renseigner vos paramètres MySQL dans `config.php`.
- Vérifier le fonctionnement avec `api-tester.html` ou un endpoint API.
